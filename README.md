# ori-releases

OpenRouterIntern's release repo

## License

Apache License 2.0 — see [LICENSE](LICENSE) and [NOTICE](NOTICE).

The grant covers everything distributed here: the manifests and version pointers committed to this repo, plus the release assets (`ori` binaries, `install.sh`, checksums, docs bundle). The binaries are compiled from a private source tree, so the license applies to those artifacts as distributed, not to any published source for them. Third-party components bundled into the binaries stay under their own licenses.
